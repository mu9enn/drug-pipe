interface:
  display_name: "H-Cluster RJob Submit"
  short_description: "Submit and size H-cluster GPU allocations in the correct L1/L2 pool"
  default_prompt: "Use $h-rjob-submit to pick the right pool and quota group for this job, then render, validate, and submit the rjob command with priority 9 and a foreground entrypoint that releases the allocation when the workload ends."
