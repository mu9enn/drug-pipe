interface:
  display_name: "Slime H-Cluster Training"
  short_description: "Configure, gate, and diagnose Slime training runs on H-cluster H200s"
  default_prompt: "Use $slime-h-cluster-training to design, gate, or diagnose this Slime SFT, ToolRL, or GAD training run. If the question is really about the rjob command itself, its pool/quota group, or the allocation lifetime, use $h-rjob-submit instead."
